/** @type {import('prettier').Options} */
const config = {
    tabWidth: 4,
    semi: false,
    singleQuote: true,
    jsxBracketSameLine: false,
    printWidth: 150,
    arrowParens: 'avoid',
}

module.exports = config
